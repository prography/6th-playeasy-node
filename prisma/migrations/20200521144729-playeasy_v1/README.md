# Migration `20200521144729-playeasy_v1`

This migration has been generated by ymink716 at 5/21/2020, 2:47:29 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `playeasy`.`match` DROP FOREIGN KEY `match_ibfk_3`,
DROP FOREIGN KEY `match_ibfk_2`,
DROP COLUMN `awayQuota`,
DROP COLUMN `away_team_id`,
DROP COLUMN `homeQuota`,
DROP COLUMN `home_team_id`,
DROP COLUMN `started_at`,
ADD COLUMN `end_at` datetime(3) NOT NULL  ,
ADD COLUMN `home_quota` int NOT NULL  ,
ADD COLUMN `start_at` datetime(3) NOT NULL  ,
ADD COLUMN `title` varchar(191) NOT NULL  ;

ALTER TABLE `playeasy`.`user` ALTER COLUMN `push_token` DROP DEFAULT,
DROP COLUMN `updated_at`,
ADD COLUMN `updated_at` datetime(3)   ;

DROP TABLE `playeasy`.`matchTeamApplication`;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200521144729-playeasy_v1
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,62 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource mysql {
+  url      = env("DB_URL")
+  provider = "mysql"
+}
+
+model User {
+  id              Int         @default(autoincrement()) @id
+  name            String?   
+  age             Int?      
+  email           String      @unique
+  socialType      String      @default("kakao") @map("social_type")
+  phone           String?   
+  pushToken       String?     @map("push_token")
+  createdAt       DateTime    @default(now()) @map("created_at")
+  updatedAt       DateTime?   @updatedAt() @map("updated_at") 
+  
+  team            Team?       @relation(fields: [teamId], references: [id])
+  teamId          Int?        @map("team_id")
+
+  @@map("user")
+}
+ 
+model Team {
+  id              Int         @default(autoincrement()) @id
+  name            String   
+  description     String?
+  users           User[]      
+
+  @@map("team")   
+}
+
+model Match {
+  id              Int         @default(autoincrement()) @id 
+  title           String      
+  type            MatchType 
+  description     String?
+  location        String   
+  fee             Int?       
+  startAt         DateTime    @map("start_at")
+  endAt           DateTime    @map("end_at")
+  homeQuota       Int         @map("home_quota")
+  writer          User        @relation(fields: [writerId], references: [id])
+  writerId        Int         @map("writer_id")
+  
+  // homeTeam        Team?       @relation( fields: [homeTeamId], references: [id]) 
+  // homeTeamId      Int?        @map("home_team_id")
+  
+
+  // awayQuota       Int?        @map("away_quota") 
+  // awayTeam        Team        @relation("away" fields: [awayTeamId], references: [id])
+  // awayTeamId      Int?        @map("away_team_id")
+  
+  @@map("match")
+}
+
+enum MatchType {
+  SOCCKER FUTSAL5 FUTSAL6
+}
```


